# Benchmark Retrospective — 20260519T010526-classic

**Cluster type:** classic  
**Date:** 2026-05-19  
**Tests run:** 01-cluster-install (complete), 15-spot-instances (complete / partial pods), 16-arm-nodes (skipped on Classic)

<!-- SUMMARY_PLACEHOLDER -->

---

## Test 01 — Cluster Install

**Status:** completed  
**Total time:** 51m 32s (`classic.total`)

### Issues Encountered

- First create attempt failed with OCM `CLUSTERS-MGMT-500`; retry on the same cluster name succeeded.
- Harness initially picked system Python 3.9 (`datetime.UTC`); reran with `.venv` (Python 3.11+).
- Transient `oc login` `401 Unauthorized` during post-ready checks; subsequent attempt succeeded (`classic.oc_login_ok`).

### Key Insights

- End-to-end time landed within the skill’s typical Classic window (~45–65 min).
- Install-log-derived milestones (`waiting_for_api`, `api_reachable`, `bootstrap_destroying`) aligned with normal ROSA installer progression.

---

## Test 15 — Spot instances

**Status:** checkpoint completed (`workload_applied_to_pods_running_partial`)  
**Report:** [`reports/20260519T010526-classic-15-classic-spot-instances.html`](../../reports/20260519T010526-classic-15-classic-spot-instances.html)  
**Canvas:** `canvases/test-15-spot-classic-20260519T010526.canvas.tsx` (project canvases folder)

### What happened

- Multi-AZ Classic requires machine-pool replicas in multiples of three; `--max-replicas` was corrected to **6** in `run-test-15-spot-instances.py`.
- CAS path behaved as expected (`ScaledUpGroup` → Machine API → EC2 Spot); **~6m27s** from CAS trigger to first spot node Ready.
- Not every workload replica reached Running before the harness wait elapsed (`_partial` milestone). Treat as infra-validated CAS/spot provisioning with workload completion still tightened in a future run.

### Baseline gap

- `on_demand_baseline_ms` was empty — no **test 03** in this `RUN_ID` for apples-to-apples CAS on-demand latency.

---

## Test 16 — ARM64 nodes

**Status:** harness **skipped** on Classic (`--cluster-type classic`, exit **0**)  
**Canvas:** `canvases/test-16-arm-classic-skip-20260519T010526.canvas.tsx`

### Reason

OCM returns **CLUSTERS-MGMT-400** for Graviton (`m6g*`) pools on ROSA Classic topology. Benchmark **16 is supported on HCP and HCP AutoNode** only; rerun there for ARM timings.
